import { Box, Container } from "@mui/material";
import PlanMeal from "../PlanMeal/PlanMeal";

function PlanPage() {
  return (
    <Container maxWidth="sm">
      <PlanMeal />
    </Container>
  );
}

export default PlanPage;
